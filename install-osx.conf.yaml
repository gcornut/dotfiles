- clean:
    - '~'
- link:
    ~/.zplug: zplug/
    ~/.zshrc: zshrc
    ~/.zsh: zsh/
    ~/.tmux.conf: tmux.conf
    ~/.vim: vim/
    ~/.vimrc: vimrc
    ~/.gitconfig: gitconfig
    ~/.gitignore: .gitignore
    ~/.gitglobalignore: gitglobalignore¬
- shell:
    - - git submodule update --init --recursive
      - Installing submodules
